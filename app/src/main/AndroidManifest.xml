<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos de Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.appMobilMatrixPlay"
        android:usesCleartextTraffic="true">
        
        <!-- SplashActivity es la actividad de inicio con loading -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- ConfigActivity para configuración del servidor -->
        <activity
            android:name=".ConfigActivity"
            android:exported="false"
            android:screenOrientation="landscape">
        </activity>
        
        <!-- WaitingRoomActivity para sala de espera -->
        <activity
            android:name=".WaitingRoomActivity"
            android:exported="false"
            android:screenOrientation="landscape">
        </activity>
        
        <!-- MainActivity para el juego Ping Pong -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="landscape">
        </activity>
    </application>

</manifest>